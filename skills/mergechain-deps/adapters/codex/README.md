# Codex adapter for mergechain-deps

Use the canonical skill directly from `skills/mergechain-deps/SKILL.md` (or the copy vendored into your project).

The skill enables agents to declare PR merge dependencies via `gh` + Node so that the MergeChain browser extension (and other teammates) see the same state.
